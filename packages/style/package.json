{
  "name": "@orion-ui/style",
  "version": "1.0.3",
  "description": "orion ui styles",
  "main": "lib/index.js",
  "scripts": {
    "disable-lib": "babel src --out-dir lib --copy-file",
    "disable-build": "NODE_ENV=production npm run lib",
    "start": "NODE_ENV=development npm run lib -- --watch"
  },
  "author": "Autodesk Inc.",
  "license": "Apache-2.0",
  "devDependencies": {
    "babel-cli": "^6.18.0"
  }
}